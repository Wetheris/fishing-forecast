diff --git a/src/types/dashboard.ts b/src/types/dashboard.ts
--- a/src/types/dashboard.ts
+++ b/src/types/dashboard.ts
@@
   | "hourly-forecast"
   | "daily-forecast"
   | "radar-map"
+  | "flow-visualizer"
   | "wind-speed"
diff --git a/src/widgets/component-registry.ts b/src/widgets/component-registry.ts
--- a/src/widgets/component-registry.ts
+++ b/src/widgets/component-registry.ts
@@
 import { RadarWidget } from "@/widgets/radar/RadarWidget";
+import { FlowVisualizationWidget } from "@/widgets/flow/FlowVisualizationWidget";
@@
   "daily-forecast": DailyForecastWidget,
   "radar-map": RadarWidget,
+  "flow-visualizer": FlowVisualizationWidget,
   "wind-speed": WindSpeedWidget,
diff --git a/src/widgets/registry.ts b/src/widgets/registry.ts
--- a/src/widgets/registry.ts
+++ b/src/widgets/registry.ts
@@
   {
     key: "radar-map",
     name: "Weather Radar",
@@
     ),
   },
+  {
+    key: "flow-visualizer",
+    name: "Wind & Tide Flow Map",
+    description:
+      "Directional map showing wind movement or modeled tide/current flow around a chosen fishing spot.",
+    category: "wind",
+    sourceKind: "weather-location",
+    defaultTitle: "Wind & Tide Flow",
+    defaultSettings: {
+      mode: "wind",
+      radiusMiles: 20,
+      density: 5,
+      showSourceLabel: false,
+      showLastUpdated: false,
+    },
+    defaultPlacement: placements(
+      {
+        w: 6,
+        h: 5,
+        minW: 4,
+        minH: 4,
+      },
+      {
+        w: 4,
+        h: 5,
+        minW: 4,
+        minH: 4,
+      },
+    ),
+  },
   {
     key: "wind-speed",
diff --git a/src/components/dashboard/DashboardCanvas.tsx b/src/components/dashboard/DashboardCanvas.tsx
--- a/src/components/dashboard/DashboardCanvas.tsx
+++ b/src/components/dashboard/DashboardCanvas.tsx
@@
-    case "radar-map":
+    case "radar-map":
+    case "flow-visualizer":
       return {
         minW: mobile ? 2 : 3,
         minH: 3,
diff --git a/src/components/builder/SelectedWidgetPanel.tsx b/src/components/builder/SelectedWidgetPanel.tsx
--- a/src/components/builder/SelectedWidgetPanel.tsx
+++ b/src/components/builder/SelectedWidgetPanel.tsx
@@
-    case "radar-map":
+    case "radar-map":
+    case "flow-visualizer":
       return {
         h: mobile ? 4 : 3,
         minH: mobile ? 4 : 3,
@@
 function WidgetSpecificSettings({
   widget,
   onChange,
@@
   if (widget.widgetKey === "forecast-overview") {
@@
   }
+
+  if (widget.widgetKey === "flow-visualizer") {
+    return (
+      <fieldset className="space-y-3">
+        <legend className="text-sm font-medium">
+          Flow visualization
+        </legend>
+
+        <SelectSetting
+          label="Default visualization"
+          value={stringSetting(
+            widget.settings,
+            "mode",
+            "wind",
+          )}
+          options={[
+            ["wind", "Wind direction"],
+            ["current", "Tide / current direction"],
+          ]}
+          onChange={(value) =>
+            onChange("mode", value)
+          }
+        />
+
+        <NumberSetting
+          label="Map radius (miles)"
+          value={numberSetting(
+            widget.settings,
+            "radiusMiles",
+            20,
+          )}
+          min={5}
+          max={60}
+          onChange={(value) =>
+            onChange("radiusMiles", value)
+          }
+        />
+
+        <SelectSetting
+          label="Arrow density"
+          value={String(
+            numberSetting(
+              widget.settings,
+              "density",
+              5,
+            ),
+          )}
+          options={[
+            ["3", "Low · 3 × 3"],
+            ["5", "Normal · 5 × 5"],
+            ["7", "High · 7 × 7"],
+          ]}
+          onChange={(value) =>
+            onChange(
+              "density",
+              Number(value),
+            )
+          }
+        />
+
+        <p className="rounded-xl bg-[var(--surface-muted)] p-3 text-xs leading-5 text-[var(--muted)]">
+          In edit mode, tap or click directly on the map
+          to choose the visualization center. Wind is the
+          default. Tide flow uses modeled ocean-current
+          direction and is not intended for navigation.
+        </p>
+      </fieldset>
+    );
+  }

   if (widget.widgetKey === "current-temperature") {
