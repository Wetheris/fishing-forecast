/**
 * The original flowing-grid card was replaced by WidgetShell and DashboardCanvas.
 * This file remains as a migration marker so extracting the overlay over an older
 * checkout does not leave code that references the removed WidgetSize model.
 */
export const dashboardWidgetCardReplaced = true;
